export enum USER_ROLE {
  "admin",
  "user",
}

export enum VIDEO_STATUS {
    "active",
    "archived",
    "deleted"
}

export enum UPLOAD_STATUS {
    "pending",
    "processing",
    "completed",
    "failed"
}

export enum FILE_TYPE {
  "thumbnail",
  "video",
  "profilePicture"
}
